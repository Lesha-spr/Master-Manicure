mixin sorting(sorting)
    form.sorting(data-module='Sorting')
        .sorting__inner
            span Выводить:
            each item in sorting
                label
                    input.sorting__input(name='sorting', type='radio', value='#{item.value}')
                    span(class='sorting__label #{item.mod}')!= item.title